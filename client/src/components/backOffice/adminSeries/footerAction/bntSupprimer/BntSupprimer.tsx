import style from "./BntSupprimer.module.css";

const BntSupprimer = () => {
  return (
    <button className={`${style.BntSupprimer}`} type="button">
      Supprimer
    </button>
  );
};

export default BntSupprimer;
